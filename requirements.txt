Flask==2.3.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-Login==0.6.3
Flask-WTF==1.2.1
python-dotenv==1.0.0
gunicorn==21.2.0
pytest==7.4.3
pytest-flask==1.3.0
coverage==7.3.2
psycopg2-binary==2.9.9  # PostgreSQL adapter for Python
pyodbc==4.0.39  # Microsoft SQL Server driver
sqlalchemy-pytds==0.3.2  # Alternative to sqlalchemy-pyodbc-azure for SQL Server

# Removed Azure CLI packages to simplify dependencies.
# These packages aren't needed for the deployment itself and are causing conflicts.
# If you need Azure CLI functionality within the app, we can add specific packages later.

# Other Azure packages that don't conflict
azure-identity==1.24.0b2  # Match the version compatible with Azure CLI
azure-mgmt-web>=6.0.0,<8.0.0